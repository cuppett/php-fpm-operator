apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-fpm.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-fpm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-fpm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
