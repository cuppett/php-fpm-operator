apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "php-chart.fullname" . }}-test-connection"
  labels:
    {{- include "php-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "php-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
